Find the command that allows you to connect via ssh on the VM.(In parallel with
the graphic session)
# requires
# sudo apt-get install openssh-client
# sude apt-get install openssh-client ii

On VM
ssh localhost

On the PC/Mac
ssh user_name@ip_address